apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: demo
  labels:
    app: api
    owner: platform-team
    env: w10
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
